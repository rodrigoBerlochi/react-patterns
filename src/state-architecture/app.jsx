import React from 'react';

import LoginContainer from './containers/loginContainer';

export const App = () => {
    return (
        <LoginContainer>
            {/* Here presentational tree regarding to Login domain */}
        </LoginContainer>
    );
}
